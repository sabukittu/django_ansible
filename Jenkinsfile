properties([pipelineTriggers([githubPush()])])

node {
    stage ('Checkout'){
        git url: 'https://github.com/sabukittu/django_ansible.git'
    }
    stage ('Build'){
        // steps
    }
    stage ('Test'){
        // steps
    }
} 
